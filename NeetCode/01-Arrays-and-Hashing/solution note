For Top K elements:
  when you try going through bucket sort using frequency in the third loop we should go through each element in reverse order and the buckets starting at the end in reverse order and try to append it in the result, 
  and make sure that the length fo the result is equal to the length of the k. 
  Hint 2 loops required at the end to go through the freq bucket

  For Group anagrams:
    the label should be kept in a tuple before putting it inside the cupboard, and the cupboard should be a defaultdict(list)
  
  For encode and decode
  i is the initial start at 0 , j = i after that there should be a loop running which we would want to run until 
    while s[j] !=#
      j = j+1
    so j has the point of "#" and i is the representation of the number which we can slice by using 
    length = int(s[i:j]) such that 0 is the number and j is the end point representing "#"
    again I start with incrementing i with 1+j as it represents the start of the word
    i = 1+j
    j = length + i
    res.append(s[i:j])
    again our initial is set to i by saying
    i = j

For products of array except self:
   prefix will create a result array with left hand side multiplication for 
  [1,2,3,4] it would look like [1,1,2,6]
  then the res will have to be multiplied by postfix of each 
  to get there we must have the postfix such that postfix *= nums[i] begining from the end to 0 and result[i] *= postfix 
  res = [24,12, 8, 6]

For Longest Consecutive Sequence
  Firstlky we change it to the set, we only start counting the length if the previous element does not exist in the set, so we start making a length chain adding
  one and seeing if the element exist in the set I do this while (num+ length) in set, I use the max(length, longest) parameter to store the longest element to make sure I select only th longest one 
